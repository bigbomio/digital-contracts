module.exports = {
    // port: 8545,
    // compileCommand: '../node_modules/.bin/truffle compile --network development',
    // testCommand: '../node_modules/.bin/truffle test --network development',
    // copyNodeModules:true,
    norpc: true,
    skipFiles: ['zeppelin', 'zos-lib', 'Migration.sol']
};